name: API CI

on:
    push:
        branches: [main, master]
    pull_request:
        branches: [main, master]
    workflow_dispatch:
    
jobs:
    check:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            
            - name: Mostrar estrutura
              run: |
                echo "Repo no runner:"
                ls -la
                echo "API:"
                ls -la projects/02-nodejs-api
